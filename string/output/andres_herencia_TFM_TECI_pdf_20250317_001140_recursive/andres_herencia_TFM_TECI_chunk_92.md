attention capacity of the model, since it is limited to compute the hidden states of the
transformer at each time step, and eventually difficult to capture complex relationships
between the input data.